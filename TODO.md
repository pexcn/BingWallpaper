# TODO：收藏夹功能设计（交接文档）

> 设计已定，尚未实现。动手前请通读，尤其是「核心不变量」一节。
> 初稿讨论于 2026-08-20；2026-09-05 重做了一版，把存储砍到最小、把网格改成虚拟化，
> 本文是重做后的版本。同批讨论出的跨市场去重、锁定壁纸都已实现，不在本文范围内。

## 目标

给程序加收藏夹：**一旦收藏，程序永远不会删除它**；用户也可以自己往收藏夹里拷图片。
收藏几千张之后，开窗、滚动仍然跟手，内存和 CPU 不随收藏总数增长。

四个约束决定了整个设计：

1. **Bing 接口只回溯 8 天**——过了窗口，图片和元数据都拿不回来。收藏必须同时持久化
   *文件本身*和*显示用的文字*，不能只存一个 ID 等以后再拉。
2. **`KeepDays` 会清理缓存**——收藏必须处在清理器的射程之外，而不是靠清理器「记得跳过」。
3. **便携性底线**——不写程序目录之外，不引入依赖，用户手改 / 手拷要能理解。
4. **规模**——收藏夹是用户自己往里拷东西的地方，条目数没有上界。任何「每条一个控件」
   「每条一张常驻位图」的做法都会在几百条时崩掉，这一条直接否掉了 WinForms 的常规写法。

## 核心不变量

任何改动都不得破坏这三条：

1. **程序代码里不存在指向 `favorites\` 下任何路径的 `File.Delete` / `FileInfo.Delete`。**
   「永不删除」应当是结构性保证，而不是某处的一句 `if (isFavorite) continue;`——后者太容易
   在将来某次重构里被绕过。这条**没有例外**：缩略图缓存放在 `favorites\` 外面，正是为了
   让这句话能说满，README 里也不用写「（某某除外）」。
   现成的保证：`WallpaperService.Cleanup` 和 `RemoveStaleResolutions` 已经是
   `SearchOption.TopDirectoryOnly`（`WallpaperService.cs:113`、`:191`），子目录天然够不着。
2. **取消收藏也不删文件**：移回 `wallpapers\`，从 `favorites.txt` 移除该行，之后按普通缓存
   的规则老去。
3. **目录是唯一事实来源。** 「是不是收藏」由「文件在不在 `favorites\`」决定；有几张、多大、
   按什么顺序排，全部来自一趟目录枚举。`favorites.txt` **只是标题缓存**，丢了只掉字不掉图。
   于是「用户自己拷文件进来」不是边缘情况，而是模型的自然结果——**不需要任何「对账」流程**，
   因为没有第二份状态需要跟目录同步。

> 已实现的 `WallpaperService.RemoveStaleResolutions`（同图旧尺寸去重）**不能套用到
> `favorites\`**，会违反第 1 条。它现在够不着，保持够不着即可。

## 目录结构

**不新建 `daily\`**：现有的 `wallpapers\` 顶层就是每日缓存，一行不用改。

```
<程序目录>\
└─ wallpapers\
   ├─ 20260818_WhyteCliffP_UHD.jpg   # 每日缓存，受 KeepDays 管辖（现状不变）
   ├─ favorites\                     # 收藏，程序永不删除这里的任何东西
   │  ├─ 20260810_MountainLake_UHD.jpg
   │  ├─ IMG_2034.jpg                # 用户自己拷进来的
   │  └─ favorites.txt               # 标题缓存，可有可无
   └─ .thumbs\                       # 缩略图缓存，可再生，程序自己的，随便删
      └─ IMG_2034.jpg.jpg
```

为什么不拆出 `daily\`：清理逻辑本来就只扫顶层，拆目录并不会让不变量 1 更牢固，却要改两处
删除逻辑、改 `ResolveWallpaperFile` 的两端、重画 README 目录树、外加说明残留文件怎么办。
保持顶层不动，收益一样，改动面小一圈。

为什么 `.thumbs\` 在 `favorites\` **外面**：让不变量 1 没有例外（见上）；顺带用户把
`favorites\` 整个拷走做备份时不会捎上一堆缓存垃圾，而 `favorites.txt` 仍在里面，元数据照样跟着走。

- **路径解析顺序**：`favorites\` → `wallpapers\` → 下载。一张图在磁盘上永远只有一份。
  集中在 `Paths.ResolveWallpaperFile`，目录一分为二时只改这一个方法。
- **收藏动作 = 从 `wallpapers\` move 到 `favorites\`**，不是复制、不是打标记。同卷 rename，
  原子且不占额外空间。`SystemParametersInfoW` 设完壁纸后不持有文件句柄（Windows 已转码到
  `TranscodedWallpaper`），移动「正在使用中」的壁纸是安全的；顺手重设一次注册表 `Wallpaper`
  路径保持记录准确。
- **不需要迁移旧文件**：新布局是现有布局的超集，顶层原样保留。

## 一个条目需要的东西，几乎全在目录里

这是把存储砍到最小的关键。列一遍每个显示字段的来源：

| 字段 | 来源 |
| --- | --- |
| 是不是收藏 | 文件在不在 `favorites\` |
| 文件名（主键） | 目录项 |
| 日期 / 排序键 | 文件名反解 `startdate`（现成的 `BingImageInfo.TryParseFileName`），解不出就用 `LastWriteTime` |
| 用户自带图的标题 | 不含扩展名的文件名 |
| 收藏时间 | `LastWriteTime`（`File.Move` 保留 mtime，所以 Bing 图 move 进来之后仍≈它当天的日期，排序天然正确） |
| 单张大小 / 合计占用 | `FileInfo.Length` |

**只有 Bing 图的标题和版权链接是目录里拿不到的**，落盘的也就只有这三个字段。

枚举用 `DirectoryInfo.EnumerateFiles` 而不是 `Directory.GetFiles`：返回的 `FileInfo` 的
`Length` / `LastWriteTime` 在枚举时就已预填，**不产生额外的 stat 调用**。于是「列目录 +
排序键 + 底部那句『共 N 张，占用 X MB』」是同一趟枚举里一次拿全的，一次系统调用都不多花。

扩展名白名单 `.jpg / .jpeg / .png / .bmp`（`SystemParametersInfoW` 在 Win10 上这几种都能吃），
其余无视。`favorites.txt` 自身排除在扫描外。

**不做入库前的解码校验。** 半截文件、改了扩展名的非图片会在生成缩略图时解码失败，画个占位
符、记一次日志即可——代价只落在用户真正滚到眼前的那几张上，而不是开窗时全量解码。

## favorites.txt

放在 `wallpapers\favorites\favorites.txt`，跟它描述的文件同处一室。**不动 `BingWallpaper.ini`。**

**不要用 JSON。** 只有三个字段要存，`JavaScriptSerializer` 只能输出压缩成一行的 JSON，
为了「可手改、可看 diff」还得自己手写一个带缩进的输出器，再配上版本号、坏文件改名、
原子重建一整套仪式——为三个字段背这些不值。一行一条的 TSV 就够，跟 `AppConfig` 手写 INI
解析是同一个调性：

```
20260810_MountainLake_UHD.jpg	晨雾中的黄山	黄山，安徽 (© Someone/Getty Images)	https://www.bing.com/search?q=...
```

- 四列：**文件名（主键，Windows 下大小写不敏感比较）**、标题、版权、版权链接。
- 读 = `File.ReadAllLines` + `Split('\t')`；写 = `string.Join`。UTF-8 无 BOM，`.tmp` + 原子改名。
- **转义规则只有一条**：写入时把标题 / 版权里的 tab 和换行剔掉。没有别的特例。
- 标题取**首次获取时那个市场**的值（同一张图在 de-DE 下标题是德文）。
- **没有 `version` / `source` / `market` / `addedAt` 字段**：`source` 是「文件名解不解析得了」
  推出来的，`addedAt` 就是 mtime，`market` 没有任何读者，版本号等到真要改格式时再说。
- **用户自己拷进来的图在这里没有行**，因为它没有任何目录里拿不到的信息。这正是「不需要对账」
  的原因：没有新条目要登记，没有孤儿条目要清理。
- **坏行直接跳过**，记一次日志，不改名、不重建、不中断。整个文件读不出来也只是所有 Bing 图
  临时退化成文件名标题。
- **不在启动时碰 `favorites\`**。窗口本来就是每次打开新建、关闭即 dispose，开窗时列一次目录
  （几千项约 10 ms，不解码任何图片）就够了。没有常驻索引，也就没有失效问题、没有脏标记、
  没有后台写盘线程。
- 写盘只发生在收藏 / 取消收藏时：整份重写一次（几千行也才几百 KB），顺手丢掉文件已不存在的行。

**写入顺序：先 move 文件，成功后写 txt。** 宁可丢标题不能丢图。这个顺序已经不是关键路径了
（丢了 txt 只丢标题），但不花钱，保持。

## 缩略图

### 磁盘缓存

`wallpapers\.thumbs\`，目录设 Hidden 属性。

- **命名**：`<含扩展名的源文件名>.jpg`，即 `IMG_2034.jpg` → `IMG_2034.jpg.jpg`，
  `a.png` → `a.png.jpg`。带上原扩展名是为了让 `a.jpg` 和 `a.png` 天然不撞名——与其加一条
  冲突检测分支，不如让特例不存在。仍然可读、可 grep、一眼对得上源文件。
- **失效**：缩略图 LastWriteTime 早于源文件就重生成（同名覆盖时能正确更新）。
- **尺寸**：长边 320 逻辑像素 × DPI 缩放，**高质量 bicubic 一步到位**（同图标 16/20/24 帧的
  判断：逐级缩小出来的东西发虚），JPEG 质量 85，约 25 KB/张。嫌占地方就降到 240px / q80。
- **孤儿清理是可选的、惰性的**：源文件没了的缩略图留着只占字节，什么时候顺手扫到什么时候删。
  这里是程序唯一可以删东西的地方。

### 生成时机与调度

- **不在启动时做**，也不在开窗时批量做。开窗只做那趟不解码的目录枚举。
- 一个**后台线程，并发度 1**。首次生成要全尺寸解码 UHD JPEG（GDI+ 会分配
  3840×2160×4 ≈ 33 MB 的中间位图），并发只会让内存峰值翻倍而不会更快。
- 任务队列是 **LIFO 栈**，键是当前可见区。快速滚动时最新可见的优先，划过去的直接丢弃，
  绝不出现「滚到底部还在补开头那 500 张」。
- UI 先占位后替换。命中磁盘缓存约 3 ms/张，所以除了第一遍，滚动基本是即时的。

> 如果哪天觉得首次生成还是太重，`net48` 里有一张不违反「零 NuGet」的牌：引用 GAC 里的
> `PresentationCore`，用 `JpegBitmapDecoder` + `DecodePixelWidth` 做 DCT 缩放解码，中间位图
> 从 33 MB 降到 ~1 MB，快 5–10 倍。代价是首次触发时把 WPF 那几个程序集加载进进程。
> **现在不要做**，先按上面的方案跑，真觉得慢了再拿实测说话。

## UI

### 托盘菜单

- 「**选择日期...**」改名为「**选择壁纸...**」。

### 壁纸选择窗口（原 HistoryForm，可考虑连类名一起改成 `PickerForm`）

顶部两个 tab：「**最近**」「**收藏**」。

**不要用 WinForms 的 `TabControl`。** 它是深色模式的老大难：即使 `DrawMode = OwnerDrawFixed`
能自绘标签项，标签条背景和页面边框仍由 comctl32 按系统主题绘制，深色下会露出浅色条；
`SetWindowTheme("", "")` 关掉主题又退回 Win95 外观。

**做法**：顶部一个**自绘分段控件**（两个互斥 toggle 按钮，选中态用强调色蓝，与项目已有的
自绘单选 / 复选框同一套画法），下方一个 `Panel` 切换内容。可控、零主题坑、风格统一。
别忘了键盘可达性：Left/Right 切换 + 焦点框。

### 网格必须虚拟化（这一条是新增的，也是最重要的）

**现有的「每个条目一个 `ThumbnailTile` 控件塞进 `FlowLayoutPanel`」的写法不能沿用到收藏夹。**
最近 tab 固定 8 个所以没事；收藏夹 2000 个就是 2000 个 HWND：

- 创建 2000 个控件句柄外加一趟 `FlowLayoutPanel` 布局，开窗要卡几秒——**跟缩略图有没有缓存
  无关，第二次打开一样慢**；
- 每个控件自带消息队列开销和 desktop heap 占用；
- `ThumbnailTile.Thumbnail` 持有的是解码后的 `Bitmap`，200×112×4B ≈ 90 KB，2000 张 = 180 MB，
  而且没有任何东西会释放不可见磁贴的位图。

**改成一个自绘的滚动 `Panel`**：

- 条目是内存里的 struct（文件名 + 标题 + 排序键 + 大小，约 200 B/条），5000 条约 1 MB；
- `OnPaint` 只画可见行，命中测试自己算，hover / 焦点 / 键盘导航自己维护；
- 位图字典**只保留可见区 ± 一屏**，滑出范围即 `Dispose`。约 60 张 × 90 KB ≈ 5 MB，
  **与收藏总数无关**。

这不是净增代码：它替掉整个 `UI/ThumbnailTile.cs`（314 行）和 `HistoryForm.Populate` 里那套
增删磁贴的生命周期。做成「网格控件 + 条目数据源接口」（条目数、标题、按索引取缩略图的异步
方法），**最近 tab 和收藏 tab 共用同一份绘制 / 命中测试 / 键盘逻辑**，只是数据源一个走网络
字节（现有的 `ThumbnailCache`）、一个走本地磁盘缓存。一套网格，两个源。

**同时要去掉 `WS_EX_COMPOSITED`**（`UI/HistoryForm.cs:100`）。那段注释写得很清楚，「窗口永不
滚动所以不欠这个代价」——收藏 tab 一滚动，前提就没了，代价立刻要还：合成窗口的滚动更新会明显
发滞，严重时 WM_PAINT 空转烧 CPU。而它当初要解决的两个闪烁源——磁贴一个个到达、状态栏被
hover 改写——在「整个网格是一个自绘控件」之后本来就不存在了。改为给网格控件开
`ControlStyles.OptimizedDoubleBuffer`。

窗口也不能再是 `FixedDialog`：收藏 tab 需要能拉大。`FitToGrid` 保留为初始尺寸的计算，
最近 tab 的 4×2 仍然一眼看全。

### 内容区

- 「最近」= 现有的最近 8 天网格，行为不变，只是换了实现。
- 「收藏」= 同一个网格，按日期倒序，纵向滚动。
- **交互**：点击 = 设为壁纸（沿用现有行为）；右键菜单 = 设为壁纸 / 取消收藏 / 打开版权链接 /
  在资源管理器中显示。没有 `favorites.txt` 行的条目标题显示文件名、版权链接项禁用。
- **星标只出现在「最近」tab**，标记「这张已经收藏过了」（按文件名在收藏集合里查）。
  收藏 tab 里每一项都是收藏，不需要星标，取消收藏走右键菜单。这样也顺手化解了原设计里
  「磁贴右上角已被『当前』/『已锁定』徽章占用」的位置冲突：星标放左下角，互不打架。
- 收藏 tab 底部显示「共 N 张，占用 X MB」——就在那趟目录枚举里，不额外扫盘。
  不主动清理，但给用户判断的依据（一年 4K 约 1–2 GB）。

## 预期开销

| 项 | 量级 |
| --- | --- |
| 条目内存 | 约 200 B/条，5000 条 ≈ 1 MB |
| 位图内存 | 可见区 ± 一屏，约 60 张 ≈ 5 MB，**与总数无关** |
| 磁盘缩略图 | 25 KB/张，2000 张 ≈ 50 MB |
| 开窗成本 | 一趟目录枚举 + 一次 `ReadAllLines`，约 10 ms，**不解码任何图片** |
| 滚动成本 | 一个后台线程，LIFO 取任务，命中磁盘缓存约 3 ms/张 |
| 窗口关闭后 | 零。无 `FileSystemWatcher`、无定时器、无常驻索引 |

**性能上的底线**：单次收藏 / 取消至多重写一次 txt、原子替换一次，绝不逐条写盘；上千条时打开
窗口不该有可感知的卡顿。这是结构要求，不设墙钟阈值——真觉得慢了，先拿实际样本剖析确认瓶颈
在哪，再谈换方案。

## 与锁定壁纸的交界

锁定已实现（`PinnedWallpaper`），两个功能刻意互不知情，动工时只需记住：

- INI 里存的是**裸文件名**而非路径，正是为了「收藏 = move 到 `favorites\`」不会让锁定失效；
  路径解析集中在 `Paths.ResolveWallpaperFile`，目录一分为二时只改这一个方法。
- `Cleanup` / `RemoveStaleResolutions` 的保护参数已是 `IReadOnlyCollection<string>?`，不必再改签名；
  两者的枚举范围也已经是顶层，不必再改。
- 锁定的图出了 8 天窗口后进入游离态，标题靠 `BingImageInfo.TryParseFileName` 反解。收藏夹
  上线后，这种条目可以改从 `favorites.txt` 取回完整标题和版权链接。
- **锁定 ≠ 收藏**：锁定只保护当前这一张，换了锁定对象旧的照常按 `KeepDays` 老去。别让锁定
  长成半个收藏夹。

## 明确不做

- **不做「目录对账」**。它在原设计里存在，是因为 JSON 被当成了需要跟目录同步的数据库。
  现在目录就是状态，没有第二份东西需要对齐。
- **收藏不参与托盘的「上一张 / 下一张」**，那仍是最近 8 天内切换。两套序列混在一起语义会乱；
  「从收藏中随机轮换」是另一个功能，要做也是单独的开关。
- **不挂 `FileSystemWatcher`**。实时刷新要处理「拷贝没完事件先到」的独占重试和去抖，三个坑
  换一个「窗口正开着时用户往目录里拖文件」的场景，不值。开窗扫一次已经够，加个 F5 手动刷新
  更顺理成章——毕竟列目录已经是唯一的状态获取方式。
- **不为拷进来的文件回接口补标题**。收益是一行文字，代价是在读取流程里插一条异步网络路径
  和它的失败分支。文件名反解出的日期足够显示了。
- **内容哈希去重**：现有命名防不住「同一张图在不同日期下发到不同市场」，会存两份。少见，
  可接受。真要做，前提是先拿三张跨市场同图 `certutil -hashfile` 验证字节一致（id 不同意味着
  可能是不同 CDN 对象），且 `favorites\` 只能提示用户点、不能自动删。

## 涉及的改动面

**新增**

- `Favorites.cs`——列目录、TSV 读写、收藏 / 取消收藏（move + 重写一行）。约 150 行。
- `ThumbnailStore.cs`——本地大图解码 + `.thumbs\` 磁盘缓存 + 后台 LIFO 队列。
  注意**不是**改现有的 `ThumbnailCache.cs`：那个缓存的是从接口下载来的缩略图字节，是另一件事，
  作为「最近」tab 的数据源保留。
- `UI/TileGrid.cs`——虚拟化自绘网格，带数据源接口。

**删除**

- `UI/ThumbnailTile.cs`（314 行），由 `TileGrid` 的绘制逻辑取代。
- `UI/HistoryForm.cs` 里的磁贴增删逻辑、`WS_EX_COMPOSITED`。

**修改**

- `Paths.cs`——`favorites\` / `.thumbs\` 两个常量，`ResolveWallpaperFile` 改成两级查找。
- `WallpaperService.cs`——**不动**。`TopDirectoryOnly` 已经把 `favorites\` 挡在外面。
- `UI/HistoryForm.cs`——分段控件 + 双视图，窗口改为可缩放。
- `UI/TrayContext.cs`——菜单项改名。
- `README.md`——「目录布局」加上 `wallpapers\favorites\` 和 `wallpapers\.thumbs\`；
  「本程序改动了什么」表里加这两项和 `favorites.txt`，写明**程序永不删除 `favorites\` 下的
  任何文件**（这句现在可以说满，不带例外）；特性列表加一条收藏夹。
