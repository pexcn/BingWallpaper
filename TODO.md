# TODO：收藏夹功能设计（交接文档）

> 本文是设计讨论的结论，尚未实现。实现前请通读，尤其是「核心不变量」一节。
> 讨论日期：2026-08-20。

## 目标

给程序加收藏夹：**一旦收藏，程序永远不会删除它**。

三个约束决定了整个设计：

1. **Bing 接口只回溯 8 天**——过了窗口，图片和元数据（标题、版权、版权链接）都拿不回来。
   所以收藏必须同时持久化*文件本身*和*元数据*，不能只存一个 ID 等以后再拉。
2. **`KeepDays` 会清理缓存**——收藏必须处在清理器的射程之外，而不是靠清理器「记得跳过」。
3. **便携性底线**——不写程序目录之外，不引入依赖，用户手改 / 手拷要能理解。

## 核心不变量

实现时任何改动都不得破坏这三条：

1. **程序代码里不存在指向 `wallpapers\favorites\` 图片文件的 `File.Delete`。**
   「永不删除」是结构性保证，不是某处的一句 `if (isFavorite) continue;`——后者太容易在将来
   某次重构里被绕过。唯一例外是 `.thumbs\`（可再生缓存，见下）。
2. **取消收藏也不删文件**：把文件移回 `daily\`，从 JSON 移除条目。之后它按普通缓存的规则老去。
3. **目录是唯一事实来源，JSON 只是旁挂元数据。**
   「是不是收藏」由「文件在不在 `favorites\`」决定；JSON 回答的是「这张图叫什么、哪天的、
   版权是谁」。因此「用户自己拷文件进来」不是需要特殊支持的边缘情况，而是模型的自然结果。

## 目录重构

```
<程序目录>\
├─ BingWallpaper.exe
├─ BingWallpaper.ini
├─ BingWallpaper.log
└─ wallpapers\
   ├─ daily\                      # 每日缓存，受 KeepDays 管辖
   │  └─ zh-CN_20260818_UHD.jpg
   └─ favorites\                  # 收藏，程序永不删除
      ├─ favorites.json
      ├─ zh-CN_20260810_UHD.jpg
      ├─ IMG_2034.jpg             # 用户自己拷进来的
      └─ .thumbs\                 # 缩略图缓存，可再生、允许程序删除
         └─ IMG_2034_thumb.jpg    # 命名格式为 源文件名_thumb.jpg
```

命名选了 `daily`（曾考虑 `cache`：名字本身就在说「会被删」，与 `favorites` 形成策略对仗；
但 `daily / favorites` 更贴合产品概念，「会被清理」交给 README 讲清楚）。

- **路径解析顺序**：`favorites\` → `daily\` → 下载。一张图在磁盘上永远只有一份。
- **收藏动作 = 从 `daily\` move 到 `favorites\`**，不是复制、不是打标记。同卷 rename，
  原子且不占额外空间。`SystemParametersInfoW` 设完壁纸后不持有文件句柄（Windows 已转码到
  `TranscodedWallpaper`），所以移动「正在使用中」的壁纸是安全的；顺手重设一次注册表
  `Wallpaper` 路径保持记录准确。
- **迁移**：启动时若 `wallpapers\` 顶层还有 `*.jpg`，一次性移进 `daily\` 并记日志。老用户无感。
  注册表里的 `Wallpaper` 值会指向旧路径，但当前显示用的是转码副本，不受影响。

## favorites.json

放在 `wallpapers\favorites\favorites.json`——跟它描述的文件同处一室，整个目录拷走就是一份
完整的、自带元数据的收藏备份。**不动 `BingWallpaper.ini`。**

```json
{
  "version": 1,
  "items": [
    {
      "file": "zh-CN_20260810_UHD.jpg",
      "source": "bing",
      "market": "zh-CN",
      "startdate": "20260810",
      "title": "晨雾中的黄山",
      "copyright": "黄山，安徽 (© Someone/Getty Images)",
      "copyrightlink": "https://www.bing.com/search?q=...",
      "addedAt": "2026-08-20T14:03:11+08:00"
    },
    {
      "file": "IMG_2034.jpg",
      "source": "user",
      "title": "IMG_2034",
      "addedAt": "2026-08-19T09:20:00+08:00"
    }
  ]
}
```

- **主键是 `file`**（文件名，Windows 下大小写不敏感比较）。不用 `market + startdate`——
  用户自带的图没有这两个字段，而文件名是目录里唯一必然存在的东西。
- **读**用现成的 `JavaScriptSerializer`。
- **写**要自己来：`JavaScriptSerializer` 只输出压缩成一行的 JSON，跟「纯文本可手改」的调性
  冲突，也不利于 git 备份看 diff。手写一个带缩进的输出器很小（JSON 字符串转义规则就
  `"`、`\`、控制字符三类），UTF-8 无 BOM，`.tmp` + 原子改名。
- **解析失败不许静默重建**：把坏文件改名成 `favorites.json.bad`，从目录扫描重建一份新的，
  记日志。用户手改出错不能变成元数据全丢。
- 只在真的有变化时才落盘（脏标记）；对账扫描本身不产生写入。

## 目录对账（用户直接拷文件进来）

### 触发时机

- **启动时**一次，**每次打开壁纸选择窗口**时一次。就是一趟 `Directory.GetFiles` 加内存比对，
  几百条毫无压力，**不解码任何图片**，所以不影响启动速度。
- 窗口开着时额外挂 `FileSystemWatcher` 做实时刷新（拖文件进去，网格里立刻出现）。锦上添花，
  watcher 失灵时关掉重开也能看到。
  - **坑**：拷贝大文件时事件会先于拷贝完成触发。收到事件后先尝试独占打开文件，失败就退避
    重试几秒，成功了再解码校验、入库。
  - 事件做 ~500ms 去抖，一次拖进 50 张不会触发 50 轮重写。

### 发现新文件时怎么补元数据（逐级降级）

1. **文件名符合 `<market>_<startdate>_<res>.jpg`** → 反解出 market / startdate / resolution，
   `source: "bing"`。这覆盖了「从另一台机器的 favorites 目录拷过来」和「我们自己 move 进来
   但写 JSON 失败」两种情况。
2. **且 startdate 还在最近 8 天窗口内** → 顺便向接口补齐 title / copyright / copyrightlink。
   过了窗口就认了，标题留空。
3. **完全不符合** → `source: "user"`，`title` 取不含扩展名的文件名，`addedAt` 取文件的
   **最后写入时间**（比「发现时间」更接近用户心里的收藏时间），没有版权链接。

入库前**必须先解码校验**，跟下载路径同一套防御。校验不过（半截文件、改了扩展名的非图片）
→ 不入库、不删除、只记一次日志。

扩展名白名单：`.jpg / .jpeg / .png / .bmp`（`SystemParametersInfoW` 在 Win10 上这几种都能吃）。
其他一律无视。`.thumbs\` 和 `favorites.json` 自身排除在扫描之外。

### 文件消失了怎么办（重要的不对称）

- **UI 里主动取消收藏** → 文件移回 `daily\`，JSON 条目删除。明确的用户意图。
- **扫描发现文件不见了**（手删、临时移走、同步工具还没拉下来）→ **JSON 条目保留，不删。**

理由：条目才几百字节，而它是那张图**唯一可能存在的元数据副本**——Bing 早就不提供了。
用户哪天从备份把图拷回来，标题和版权链接会自动重新挂上。孤儿条目在 UI 上不显示。

### 写入顺序

收藏动作**先 move 文件，成功后写 JSON**。宁可丢标题不能丢图。有了目录对账之后，
「move 成功 / JSON 失败」的后果已从「丢收藏」降级为「下次启动自动补回，可能少个标题」。

## 缩略图缓存

`wallpapers\favorites\.thumbs\`，目录设 Hidden 属性。

- **命名**：`<原文件全名>.jpg`，即 `IMG_2034.png` → `IMG_2034.png.jpg`。带扩展名避免
  `a.jpg` 和 `a.png` 撞车。难看，但可读、可 grep、能一眼对上源文件，比哈希更合项目风格。
- **失效**：缩略图 LastWriteTime 早于源文件就重生成（用户用同名文件覆盖时能正确更新）。
- **生成时机**：**不在启动时做**。启动只做那趟不解码的对账；缩略图在收藏 tab 首次显示时
  按需生成，后台单线程队列，UI 先占位后替换。500 张图第一次打开会慢一会儿，之后全部命中。
- **尺寸**：长边 320 逻辑像素 × DPI 缩放，**高质量 bicubic 一步重采样到位**（同图标 16/20/24
  帧的判断：逐级缩小出来的东西发虚），JPEG 质量 85。
- **`.thumbs\` 是程序唯一可以删东西的地方**：源文件没了的孤儿缩略图在对账时清掉。
  因此 README 里那句「永不删除」必须写成**覆盖图片文件、不覆盖 `.thumbs\`**，否则话说满了。
- `daily\` 视图固定 8 张，维持现状不接缓存，避免「缓存目录跟着清理策略走」的耦合；
  两边共用同一个重采样函数即可。

## UI

### 托盘菜单

- 「**选择日期…**」改名为「**选择壁纸…**」。
- 当前壁纸项下面加一条「★ 收藏当前壁纸 / 取消收藏」切换项。

### 壁纸选择窗口（原 HistoryForm）

顶部两个 tab：「**最近**」「**收藏**」。

**不要用 WinForms 的 `TabControl`。** 它是深色模式的老大难：即使
`DrawMode = OwnerDrawFixed` 能自绘标签项，标签条背景和页面边框仍由 comctl32 按系统主题绘制，
在深色下会露出浅色条；`SetWindowTheme("", "")` 关掉主题又会退回 Win95 外观。

**推荐做法**：顶部放一个**自绘的分段控件**（两个互斥的 toggle 按钮，选中态用强调色蓝，
与项目里已有的自绘单选 / 复选框同一套画法），下方一个 `Panel` 切换内容。可控、零主题坑、
跟现有 `ThemedControls` 风格一致。别忘了键盘可达性：Left/Right 切换 + 焦点框。

**内容区**：

- 「最近」= 现有的最近 8 天缩略图网格，行为不变。
- 「收藏」= 同样的网格，按日期倒序（`source: "user"` 的按 `addedAt` 排），纵向滚动。
- `ThumbnailTile` 右上角加星标叠加，点击即切换收藏；右键菜单：设为壁纸 / 取消收藏 /
  打开版权链接 / 在资源管理器中显示。`source: "user"` 的条目标题显示文件名、版权链接项禁用。
- 收藏 tab 底部显示「共 N 张，占用 X MB」——不主动清理，但让用户自己有判断依据
  （一年 4K 约 1–2 GB）。

### 明确不做

收藏不参与托盘的「上一张 / 下一张」，那仍然是最近 8 天内切换。两套序列混在一起语义会乱；
「从收藏中随机轮换」是另一个功能，要做也是单独的开关。

## 待定问题

**用户能把任意图片设成壁纸之后，「每小时检查今日壁纸」和「我就想一直用这张收藏」会打架**
——收藏的图设上去，下一个整点可能就被今日图换掉。

这个矛盾现在就存在（从历史窗口选了 8 天前那张之后同理），但收藏功能会让它变得很显眼。
**实现前先确认现有代码是怎么处理的**；如果没有专门处理，建议配套加一个
「锁定当前壁纸（暂停自动更换）」——托盘一个勾选项，状态存 INI。是否放进这一轮待定。

## 涉及的改动面

- **新增** `src/BingWallpaper/FavoritesStore.cs`——JSON 读写、目录对账、收藏 / 取消收藏。
- **新增** 缩略图缓存逻辑（可放 `UI/ThumbnailCache.cs`）。
- `Paths.cs`——新增 `daily\` / `favorites\` / `.thumbs\` 常量与迁移逻辑。
- `WallpaperService.cs`——路径解析顺序、清理器只扫 `daily\`。
- `UI/HistoryForm.cs`——分段控件 + 双视图（可考虑连类名一起改成 `PickerForm`）。
- `UI/ThumbnailTile.cs`——星标叠加、右键菜单、占位态。
- `UI/TrayContext.cs`——菜单项改名、收藏切换项。
- `README.md`——「目录布局」改成新结构；「本程序改动了什么」表里加 `wallpapers\daily\`、
  `wallpapers\favorites\`、`favorites.json`，并写明**程序永不删除 favorites 下的图片文件
  （`.thumbs\` 缓存除外）**；特性列表加一条收藏夹。
