# TODO：收藏夹功能设计（交接文档）

> 设计已定，尚未实现。动手前请通读，尤其是「核心不变量」一节。
> 讨论日期：2026-08-20。同批讨论出的跨市场去重、锁定壁纸都已实现，不在本文范围内。

## 目标

给程序加收藏夹：**一旦收藏，程序永远不会删除它**。

三个约束决定了整个设计：

1. **Bing 接口只回溯 8 天**——过了窗口，图片和元数据都拿不回来。收藏必须同时持久化
   *文件本身*和*元数据*，不能只存一个 ID 等以后再拉。
2. **`KeepDays` 会清理缓存**——收藏必须处在清理器的射程之外，而不是靠清理器「记得跳过」。
3. **便携性底线**——不写程序目录之外，不引入依赖，用户手改 / 手拷要能理解。

## 核心不变量

任何改动都不得破坏这三条：

1. **程序代码里不存在指向 `favorites\` 图片文件的 `File.Delete`。**
   「永不删除」应当是结构性保证，而不是某处的一句 `if (isFavorite) continue;`——后者太容易
   在将来某次重构里被绕过。唯一例外是 `.thumbs\`（可再生缓存）。
2. **取消收藏也不删文件**：移回 `daily\`，从 JSON 移除条目，之后按普通缓存的规则老去。
3. **目录是唯一事实来源，JSON 只是旁挂元数据。**
   「是不是收藏」由「文件在不在 `favorites\`」决定；JSON 只回答「这张图叫什么、哪天的、
   版权是谁」。于是「用户自己拷文件进来」不是边缘情况，而是模型的自然结果。

> 已实现的 `WallpaperService.RemoveStaleResolutions`（同图旧尺寸去重）**不能套用到
> `favorites\`**，会违反第 1 条。

## 目录结构

```
<程序目录>\
└─ wallpapers\
   ├─ daily\                      # 每日缓存，受 KeepDays 管辖
   │  └─ 20260818_WhyteCliffP_UHD.jpg
   └─ favorites\                  # 收藏，程序永不删除
      ├─ favorites.json
      ├─ 20260810_MountainLake_UHD.jpg
      ├─ IMG_2034.jpg             # 用户自己拷进来的
      └─ .thumbs\                 # 缩略图缓存，可再生、允许程序删除
         └─ IMG_2034.jpg_thumb.jpg
```

- **路径解析顺序**：`favorites\` → `daily\` → 下载。一张图在磁盘上永远只有一份。
- **收藏动作 = 从 `daily\` move 到 `favorites\`**，不是复制、不是打标记。同卷 rename，原子且
  不占额外空间。`SystemParametersInfoW` 设完壁纸后不持有文件句柄（Windows 已转码到
  `TranscodedWallpaper`），移动「正在使用中」的壁纸是安全的；顺手重设一次注册表 `Wallpaper`
  路径保持记录准确。
- **不需要迁移旧文件**：程序尚未发布。`wallpapers\` 顶层的残留手动删掉即可，不写兼容代码。

## favorites.json

放在 `wallpapers\favorites\favorites.json`——跟它描述的文件同处一室，整个目录拷走就是一份
自带元数据的完整备份。**不动 `BingWallpaper.ini`。**

```json
{
  "version": 1,
  "items": [
    {
      "file": "20260810_MountainLake_UHD.jpg",
      "source": "bing",
      "imageId": "MountainLake",
      "market": "zh-CN",
      "startdate": "20260810",
      "title": "晨雾中的黄山",
      "copyright": "黄山，安徽 (© Someone/Getty Images)",
      "copyrightlink": "https://www.bing.com/search?q=...",
      "addedAt": "2026-08-20T14:03:11+08:00"
    },
    {
      "file": "IMG_2034.jpg",
      "source": "user",
      "title": "IMG_2034",
      "addedAt": "2026-08-19T09:20:00+08:00"
    }
  ]
}
```

- **主键是 `file`**（Windows 下大小写不敏感比较）。用户自带的图没有 `imageId` / `startdate`，
  而文件名是目录里唯一必然存在的东西。
- `market` / `title` / `copyright` 取**首次获取时那个市场**的值（同一张图在 de-DE 下标题是德文）。
- **读**用现成的 `JavaScriptSerializer`；**写**要自己来——它只输出压缩成一行的 JSON，跟
  「纯文本可手改」的调性冲突，也不利于看 diff。手写一个带缩进的输出器很小（转义只有
  `"`、`\`、控制字符三类），UTF-8 无 BOM，`.tmp` + 原子改名。
- **解析失败不许静默重建**：坏文件改名成 `favorites.json.bad`，从目录扫描重建，记日志。
- 启动读一次建索引（按 `file` 查找、按显示日期排序），之后开窗、画磁贴、处理右键菜单只访问
  内存，不为单个条目重复解析。写盘走脏标记，在后台线程做完再切回 UI；无变化的扫描不产生写入。
- **性能上的底线**：单次收藏 / 取消至多序列化一次、原子替换一次，绝不逐条写盘；上千条时打开
  窗口不该有可感知的卡顿。这是结构要求，不设墙钟阈值——真觉得慢了，先拿实际样本剖析确认瓶颈
  在解析上，再谈换数据格式。

## 目录对账（用户直接拷文件进来）

**启动时**和**每次打开壁纸选择窗口**各一次。一趟 `Directory.GetFiles` 加内存比对，几百条毫无
压力，**不解码任何图片**，不影响启动速度。

### 发现新文件时怎么补元数据

1. **文件名符合 `<startdate>_<imageId>_<res>.jpg`** → 反解出三个字段，`source: "bing"`，标题留空。
   覆盖「从另一台机器拷过来」和「我们自己 move 进来但写 JSON 失败」两种情况。
2. **不符合** → `source: "user"`，`title` 取不含扩展名的文件名，`addedAt` 取文件的
   **最后写入时间**（比「发现时间」更接近用户心里的收藏时间），无版权链接。

入库前**必须先解码校验**，跟下载路径同一套防御。校验不过（半截文件、改了扩展名的非图片）
→ 不入库、不删除、只记一次日志。

扩展名白名单 `.jpg / .jpeg / .png / .bmp`（`SystemParametersInfoW` 在 Win10 上这几种都能吃），
其余无视。`.thumbs\` 和 `favorites.json` 自身排除在扫描外。

### 文件消失了怎么办

- **UI 里主动取消收藏** → 移回 `daily\`，删 JSON 条目。明确的用户意图。
- **扫描发现文件不见了**（手删、临时移走、同步工具还没拉下来）→ 删除对应 JSON 条目，并清理
  其孤儿缩略图。文件日后重新出现时，按「发现新文件」流程重新入库。

### 写入顺序

**先 move 文件，成功后写 JSON**。宁可丢标题不能丢图。有了目录对账，「move 成功 / JSON 失败」
的后果已从「丢收藏」降级为「下次启动自动补回，可能少个标题」。

## 缩略图缓存

`wallpapers\favorites\.thumbs\`，目录设 Hidden 属性。

- **命名**：`<含扩展名的源文件名>_thumb.jpg`，即 `IMG_2034.jpg` → `IMG_2034.jpg_thumb.jpg`。
  带上扩展名是为了让 `a.jpg` 和 `a.png` 天然不撞名——与其加一条冲突检测分支，不如让特例
  不存在。仍然可读、可 grep、一眼对得上源文件。
- **失效**：缩略图 LastWriteTime 早于源文件就重生成（同名覆盖时能正确更新）。
- **生成时机**：**不在启动时做**。启动只做那趟不解码的对账；缩略图在收藏 tab 首次显示时按需
  生成，后台单线程队列，UI 先占位后替换。500 张图第一次打开会慢一会儿，之后全部命中。
- **尺寸**：长边 320 逻辑像素 × DPI 缩放，**高质量 bicubic 一步到位**（同图标 16/20/24 帧的
  判断：逐级缩小出来的东西发虚），JPEG 质量 85。
- **`.thumbs\` 是程序唯一可以删东西的地方**：源文件没了的孤儿缩略图在对账时清掉。因此 README
  里那句「永不删除」要写成**覆盖图片文件、不覆盖 `.thumbs\`**，否则话说满了。
- `daily\` 视图固定 8 张，维持现状不接磁盘缓存，避免「缓存目录跟着清理策略走」的耦合；两边
  共用同一个重采样函数即可。

## UI

### 托盘菜单

- 「**选择日期...**」改名为「**选择壁纸...**」。

### 壁纸选择窗口（原 HistoryForm）

顶部两个 tab：「**最近**」「**收藏**」。

**不要用 WinForms 的 `TabControl`。** 它是深色模式的老大难：即使 `DrawMode = OwnerDrawFixed`
能自绘标签项，标签条背景和页面边框仍由 comctl32 按系统主题绘制，深色下会露出浅色条；
`SetWindowTheme("", "")` 关掉主题又退回 Win95 外观。

**推荐做法**：顶部一个**自绘分段控件**（两个互斥 toggle 按钮，选中态用强调色蓝，与项目已有
的自绘单选 / 复选框同一套画法），下方一个 `Panel` 切换内容。可控、零主题坑、风格统一。
别忘了键盘可达性：Left/Right 切换 + 焦点框。

**内容区**：

- 「最近」= 现有的最近 8 天缩略图网格，行为不变。
- 「收藏」= 同样的网格，按日期倒序（`source: "user"` 的按 `addedAt` 排），纵向滚动。
- `ThumbnailTile` 加星标叠加，点击即切换收藏；右键菜单：设为壁纸 / 取消收藏 / 打开版权链接 /
  在资源管理器中显示。`source: "user"` 的条目标题显示文件名、版权链接项禁用。
  - **注意**：磁贴右上角已被锁定态徽章占用（「当前」/「已锁定」），星标要另找位置或与之合并。
- 收藏 tab 底部显示「共 N 张，占用 X MB」——大小在对账那趟 `FileInfo` 里顺手就有，不额外扫盘。
  不主动清理，但给用户判断的依据（一年 4K 约 1–2 GB）。

## 与锁定壁纸的交界

锁定已实现（`PinnedWallpaper`），两个功能刻意互不知情，动工时只需记住：

- INI 里存的是**裸文件名**而非路径，正是为了「收藏 = move 到 `favorites\`」不会让锁定失效；
  路径解析集中在 `Paths.ResolveWallpaperFile`，目录一分为二时只改这一个方法。
- `Cleanup` / `RemoveStaleResolutions` 的保护参数已是 `IReadOnlyCollection<string>?`，不必再改签名。
- 锁定的图出了 8 天窗口后进入游离态，标题靠 `BingImageInfo.TryParseFileName` 反解（也正是上面
  「对账降级第 1 档」要的那个函数）。收藏夹上线后，这种条目可以改从 `favorites.json` 取回
  完整标题和版权链接。
- **锁定 ≠ 收藏**：锁定只保护当前这一张，换了锁定对象旧的照常按 `KeepDays` 老去。别让锁定
  长成半个收藏夹。

## 明确不做

- **收藏不参与托盘的「上一张 / 下一张」**，那仍是最近 8 天内切换。两套序列混在一起语义会乱；
  「从收藏中随机轮换」是另一个功能，要做也是单独的开关。
- **不挂 `FileSystemWatcher`**。实时刷新要处理「拷贝没完事件先到」的独占重试和去抖，三个坑
  换一个「窗口正开着时用户往目录里拖文件」的场景，不值。启动 + 开窗各扫一次已经够；真需要
  就加个 F5 手动刷新。
- **不为拷进来的文件回接口补标题**。收益是一行文字，代价是在对账流程里插一条异步网络路径
  和它的失败分支。文件名反解出的日期足够显示了。
- **内容哈希去重**：现有命名防不住「同一张图在不同日期下发到不同市场」，会存两份。少见，
  可接受。真要做，前提是先拿三张跨市场同图 `certutil -hashfile` 验证字节一致（id 不同意味着
  可能是不同 CDN 对象），且 `favorites\` 只能提示用户点、不能自动删。

## 涉及的改动面

- **新增** `FavoritesStore.cs`——JSON 读写、目录对账、收藏 / 取消收藏。
- **新增** `FavoriteThumbnails.cs`——本地大图解码 + 磁盘缩略图缓存。
  注意**不是**改现有的 `ThumbnailCache.cs`：那个缓存的是从接口下载来的缩略图字节，是另一件事。
- `Paths.cs`——`daily\` / `favorites\` / `.thumbs\` 常量，`ResolveWallpaperFile` 改成两级查找。
- `WallpaperService.cs`——路径解析顺序、清理器只扫 `daily\`（保护参数已经是集合，不必再动签名）。
- `UI/HistoryForm.cs`——分段控件 + 双视图（可考虑连类名一起改成 `PickerForm`）。
- `UI/ThumbnailTile.cs`——星标叠加、右键菜单、占位态。
- `UI/TrayContext.cs`——菜单项改名。
- `README.md`——「目录布局」改成新结构；「本程序改动了什么」表里加 `wallpapers\daily\`、
  `wallpapers\favorites\`、`favorites.json`，写明**程序永不删除 favorites 下的图片文件
  （`.thumbs\` 缓存除外）**；特性列表加一条收藏夹。
