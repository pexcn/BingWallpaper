# TODO：收藏夹功能设计（交接文档）

> 设计已定，尚未实现。动手前请通读，尤其是「核心不变量」一节。
> 讨论日期：2026-08-20。同批讨论出的跨市场去重方案已实现，不在本文范围内。

## 目标

给程序加收藏夹：**一旦收藏，程序永远不会删除它**。

三个约束决定了整个设计：

1. **Bing 接口只回溯 8 天**——过了窗口，图片和元数据都拿不回来。收藏必须同时持久化
   *文件本身*和*元数据*，不能只存一个 ID 等以后再拉。
2. **`KeepDays` 会清理缓存**——收藏必须处在清理器的射程之外，而不是靠清理器「记得跳过」。
3. **便携性底线**——不写程序目录之外，不引入依赖，用户手改 / 手拷要能理解。

## 核心不变量

任何改动都不得破坏这三条：

1. **程序代码里不存在指向 `favorites\` 图片文件的 `File.Delete`。**
   「永不删除」应当是结构性保证，而不是某处的一句 `if (isFavorite) continue;`——后者太容易
   在将来某次重构里被绕过。唯一例外是 `.thumbs\`（可再生缓存）。
2. **取消收藏也不删文件**：移回 `daily\`，从 JSON 移除条目，之后按普通缓存的规则老去。
3. **目录是唯一事实来源，JSON 只是旁挂元数据。**
   「是不是收藏」由「文件在不在 `favorites\`」决定；JSON 只回答「这张图叫什么、哪天的、
   版权是谁」。于是「用户自己拷文件进来」不是边缘情况，而是模型的自然结果。

> 已实现的 `WallpaperService.RemoveStaleResolutions`（同图旧尺寸去重）**不能套用到
> `favorites\`**，会违反第 1 条。

## 目录结构

```
<程序目录>\
└─ wallpapers\
   ├─ daily\                      # 每日缓存，受 KeepDays 管辖
   │  └─ 20260818_WhyteCliffP_UHD.jpg
   └─ favorites\                  # 收藏，程序永不删除
      ├─ favorites.json
      ├─ 20260810_MountainLake_UHD.jpg
      ├─ IMG_2034.jpg             # 用户自己拷进来的
      └─ .thumbs\                 # 缩略图缓存，可再生、允许程序删除
         └─ IMG_2034_thumb.jpg
```

- **路径解析顺序**：`favorites\` → `daily\` → 下载。一张图在磁盘上永远只有一份。
- **收藏动作 = 从 `daily\` move 到 `favorites\`**，不是复制、不是打标记。同卷 rename，原子且
  不占额外空间。`SystemParametersInfoW` 设完壁纸后不持有文件句柄（Windows 已转码到
  `TranscodedWallpaper`），移动「正在使用中」的壁纸是安全的；顺手重设一次注册表 `Wallpaper`
  路径保持记录准确。
- **不需要迁移旧文件**：程序尚未发布。`wallpapers\` 顶层的残留手动删掉即可，不写兼容代码。

## favorites.json

放在 `wallpapers\favorites\favorites.json`——跟它描述的文件同处一室，整个目录拷走就是一份
自带元数据的完整备份。**不动 `BingWallpaper.ini`。**

```json
{
  "version": 1,
  "items": [
    {
      "file": "20260810_MountainLake_UHD.jpg",
      "source": "bing",
      "imageId": "MountainLake",
      "market": "zh-CN",
      "startdate": "20260810",
      "title": "晨雾中的黄山",
      "copyright": "黄山，安徽 (© Someone/Getty Images)",
      "copyrightlink": "https://www.bing.com/search?q=...",
      "addedAt": "2026-08-20T14:03:11+08:00"
    },
    {
      "file": "IMG_2034.jpg",
      "source": "user",
      "title": "IMG_2034",
      "addedAt": "2026-08-19T09:20:00+08:00"
    }
  ]
}
```

- **主键是 `file`**（Windows 下大小写不敏感比较）。用户自带的图没有 `imageId` / `startdate`，
  而文件名是目录里唯一必然存在的东西。
- `market` / `title` / `copyright` 取**首次获取时那个市场**的值（同一张图在 de-DE 下标题是德文）。
- **读**用现成的 `JavaScriptSerializer`；**写**要自己来——它只输出压缩成一行的 JSON，跟
  「纯文本可手改」的调性冲突，也不利于看 diff。手写一个带缩进的输出器很小（转义只有
  `"`、`\`、控制字符三类），UTF-8 无 BOM，`.tmp` + 原子改名。
- **解析失败不许静默重建**：坏文件改名成 `favorites.json.bad`，从目录扫描重建，记日志。
- 只在真有变化时才落盘（脏标记）；对账扫描本身不产生写入。

## 目录对账（用户直接拷文件进来）

### 触发时机

- **启动时**和**每次打开壁纸选择窗口**各一次。一趟 `Directory.GetFiles` 加内存比对，几百条
  毫无压力，**不解码任何图片**，不影响启动速度。
- 窗口开着时额外挂 `FileSystemWatcher` 做实时刷新。锦上添花，失灵了关掉重开也能看到。
  - **坑**：拷贝大文件时事件先于拷贝完成触发。收到事件后先尝试独占打开，失败就退避重试
    几秒，成功了再解码校验、入库。
  - 事件做 ~500ms 去抖，一次拖进 50 张不会触发 50 轮重写。

### 发现新文件时怎么补元数据（逐级降级）

1. **文件名符合 `<startdate>_<imageId>_<res>.jpg`** → 反解出三个字段，`source: "bing"`。
   覆盖「从另一台机器拷过来」和「我们自己 move 进来但写 JSON 失败」两种情况。
2. **且 startdate 还在 8 天窗口内** → 顺便向接口补齐 title / copyright / copyrightlink，
   过了窗口标题就留空。
3. **完全不符合** → `source: "user"`，`title` 取不含扩展名的文件名，`addedAt` 取文件的
   **最后写入时间**（比「发现时间」更接近用户心里的收藏时间），无版权链接。

入库前**必须先解码校验**，跟下载路径同一套防御。校验不过（半截文件、改了扩展名的非图片）
→ 不入库、不删除、只记一次日志。

扩展名白名单 `.jpg / .jpeg / .png / .bmp`（`SystemParametersInfoW` 在 Win10 上这几种都能吃），
其余无视。`.thumbs\` 和 `favorites.json` 自身排除在扫描外。

### 文件消失了怎么办（重要的不对称）

- **UI 里主动取消收藏** → 移回 `daily\`，删 JSON 条目。明确的用户意图。
- **扫描发现文件不见了**（手删、临时移走、同步工具还没拉下来）→ **JSON 条目保留，不删。**

理由：条目才几百字节，却是那张图**唯一可能存在的元数据副本**。用户哪天把图拷回来，标题和
版权链接会自动重新挂上。孤儿条目在 UI 上不显示。

### 写入顺序

**先 move 文件，成功后写 JSON**。宁可丢标题不能丢图。有了目录对账，「move 成功 / JSON 失败」
的后果已从「丢收藏」降级为「下次启动自动补回，可能少个标题」。

## 缩略图缓存

`wallpapers\favorites\.thumbs\`，目录设 Hidden 属性。

- **命名**：`<源文件名>_thumb.jpg`，即 `IMG_2034.jpg` → `IMG_2034_thumb.jpg`。可读、可 grep、
  一眼对得上源文件，比哈希更合项目风格。
  - 唯一的坑：`a.jpg` 和 `a.png` 会撞到同一个 `a_thumb.jpg`。这种组合极少见，检测到时退化成
    `a.png_thumb.jpg` 即可，不必为它改整套命名。
- **失效**：缩略图 LastWriteTime 早于源文件就重生成（同名覆盖时能正确更新）。
- **生成时机**：**不在启动时做**。启动只做那趟不解码的对账；缩略图在收藏 tab 首次显示时按需
  生成，后台单线程队列，UI 先占位后替换。500 张图第一次打开会慢一会儿，之后全部命中。
- **尺寸**：长边 320 逻辑像素 × DPI 缩放，**高质量 bicubic 一步到位**（同图标 16/20/24 帧的
  判断：逐级缩小出来的东西发虚），JPEG 质量 85。
- **`.thumbs\` 是程序唯一可以删东西的地方**：源文件没了的孤儿缩略图在对账时清掉。因此 README
  里那句「永不删除」要写成**覆盖图片文件、不覆盖 `.thumbs\`**，否则话说满了。
- `daily\` 视图固定 8 张，维持现状不接缓存，避免「缓存目录跟着清理策略走」的耦合；两边共用
  同一个重采样函数即可。

## UI

### 托盘菜单

- 「**选择日期…**」改名为「**选择壁纸…**」。
- 当前壁纸项下面加一条「★ 收藏当前壁纸 / 取消收藏」切换项。菜单里已经有一个「固定当前壁纸」
  勾选项，两者含义不同（一个管显示什么，一个管保留什么），措辞和位置都要让人一眼分得开。

### 壁纸选择窗口（HistoryWindow）

顶部两个 tab：「**最近**」「**收藏**」。

界面已经是 WinUI 3，深色模式不再是自绘问题：用 `SelectorBar`（或 `NavigationView`
的 `PaneDisplayMode="Top"`）加一个 `Frame` / 两个 `GridView` 切换内容即可，主题、焦点框和
键盘可达性都由控件自己负责。

**内容区**：

- 「最近」= 现有的最近 8 天缩略图网格，行为不变。
- 「收藏」= 同样的网格，按日期倒序（`source: "user"` 的按 `addedAt` 排），纵向滚动。
- 网格的 `DataTemplate` 右上角加星标叠加（与现有的「当前 / 已固定」徽标同一处），点击即切换收藏；
  用 `MenuFlyout` 做右键菜单：设为壁纸 / 取消收藏 / 打开版权链接 / 在资源管理器中显示。
  `source: "user"` 的条目标题显示文件名、版权链接项禁用。
- 收藏 tab 底部显示「共 N 张，占用 X MB」——不主动清理，但给用户判断的依据（一年 4K 约 1–2 GB）。

### 明确不做

收藏不参与托盘的「上一张 / 下一张」，那仍是最近 8 天内切换。两套序列混在一起语义会乱；
「从收藏中随机轮换」是另一个功能，要做也是单独的开关。

## 已解决：固定壁纸（不再是待定问题）

原来这里记的是「每小时检查今日壁纸会跟我就想一直用这张打架」。查过了：旧代码完全没有处理，
`RefreshAsync` 无条件 `ApplyIndexAsync(0)`。**已单独实现并合入**，收藏夹这一轮不必再管它。

已定的规则：

| 动作 | 固定开关 | 固定的是哪张 |
|---|---|---|
| 选择窗口里点一张 | 打开 | 点的那张 |
| 托盘「固定当前壁纸」 | 打开 | 桌面上这张 |
| 托盘再点一次 | 关闭 | 立即回今日图并重置定时器 |
| 上一张 / 下一张 | 不变 | 已固定时跟着换，未固定时不产生固定 |
| 改市场 / 分辨率 | 自动关闭 | 走完整刷新 |
| 立即刷新 | 不变 | 不变（只更新列表） |

实现要点，收藏夹动工时会碰到：

- 状态是 INI 里的 `PinnedWallpaper=<裸文件名>`，空串 = 未固定。**存文件名而不是路径**，正是为了
  「收藏 = 从 `daily\` move 到 `favorites\`」不会让固定失效——两个功能因此互不知情。
- 路径解析集中在 `Paths.ResolveWallpaperFile`，目录一分为二时只改这一个方法。
- `WallpaperService.Cleanup` / `RemoveStaleResolutions` 的保护参数已从单个 `string?` 改成
  `IReadOnlyCollection<string>?`，固定的文件是独立的一项，不依赖它恰好等于 `_appliedPath`。
- 固定的图出了 8 天窗口后进入「游离态」：`_currentIndex = -1`，标题从文件名反解
  （`BingImageInfo.TryParseFileName`，正是本文档「目录对账降级第 1 档」要的那个函数），
  「上一张」禁用、「下一张」跳到列表最旧那张。收藏夹上线后，这种条目可以改从
  `favorites.json` 取回完整标题和版权链接。
- **固定 ≠ 收藏**：固定只保护当前这一张，换了固定对象旧的照常按 `KeepDays` 老去。
  「永不删除」仍然只是收藏夹的职责，别让固定长成半个收藏夹。

收藏夹 UI 里要注意：`ThumbnailTile` 右上角的徽章已被固定态占用（「当前」/「已固定」），
星标叠加要另找位置或与之合并。

## 遗留：内容哈希去重（可选，二期）

已实现的命名方案防不住「同一张图在**不同日期**下发到不同市场」，那时仍会存两份。少见，
当时判断可接受。真要根治就对 `daily\` 按 (文件大小 → SHA-256) 分组，同组只留一个；
`favorites\` 不能自动删，只能在收藏视图里提示「发现 N 组重复，是否清理」，由用户点。

> ⚠️ 前提是同一张图在不同市场下**字节一致**（大概率同源同编码器，但 id 不同意味着可能是不同
> CDN 对象）。先拿三张跨市场的同图 `certutil -hashfile` 比一下再决定做不做。

## 涉及的改动面

- **新增** `FavoritesStore.cs`——JSON 读写、目录对账、收藏 / 取消收藏。
- **新增** `UI/ThumbnailCache.cs`——缩略图生成与缓存。
- `Paths.cs`——`daily\` / `favorites\` / `.thumbs\` 常量。
- `WallpaperService.cs`——路径解析顺序、清理器只扫 `daily\`（保护参数已经是集合，不必再动签名）。
- `UI/HistoryWindow.xaml(.cs)`——分段切换 + 双视图（可考虑连类名一起改成 `PickerWindow`）。
- `UI/HistoryItem.cs`——星标状态、右键菜单命令、占位态。
- `UI/TrayIcon.cs` 与 `UI/AppController.cs`——菜单项改名、收藏切换项。
- `README.md`——「目录布局」改成新结构；「本程序改动了什么」表里加 `wallpapers\daily\`、
  `wallpapers\favorites\`、`favorites.json`，写明**程序永不删除 favorites 下的图片文件
  （`.thumbs\` 缓存除外）**；特性列表加一条收藏夹。
