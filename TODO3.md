# TODO3：淡入淡出的实测问题

> 来源：2026-09-06 在真机上试用 `WallpaperTransition` 后报回的三个现象，加上顺带牵出的相关项。
> 行号对照当前源码核实。尚未动手修改任何文件，修完把方框打勾即可。
>
> 报告者的三条原话：
> 1. 笔记本**不插电**（降频）时，切换偶尔不带渐变；插上电基本不复现。
> 2. **疯狂点击**切换不同壁纸时完全没有渐变。期望是逐个渐变、最后停在最后选的那张。
> 3. 切换的时候界面**会卡住一会儿**。
>
> 日志里只有 INFO，没有任何 WARN——这与下面的判断一致：三条走的都是「正常完成」路径，
> 现有代码在那些地方一个字都不会写。
>
> **2026-09-06 补：文末那条前置确认已经做完，结论是「盖子每次都挂上了」**——
> 下面的判断成立，方案 A/B/C/D 全部对症，可以直接开工。

---

## 总根因

**淡出的每一帧都靠 UI 线程收 `WM_TIMER`，而 `WM_TIMER` 是消息队列里优先级最低的一种**——
`GetMessage` 只在队列里再没有投递消息、没有输入、没有绘制的时候才把它取出来。
与此同时，换壁纸这条路径上所有的重活也全压在同一条 UI 线程上。

于是「UI 线程忙」和「渐变画不出来」是同一件事的两面：卡顿到什么程度，渐变就丢失到什么程度。
三个现象都从这里长出来，修的时候也应当一起修，单修任何一条都会留下另外两条的尾巴。

当前时长参数在 `WallpaperTransition.cs:62`、`65`、`71`：
`HoldMilliseconds = 120` + `FadeMilliseconds = 380`，肉眼看到的过渡是 **380 ms**，整件事 500 ms。

---

## P0 · 用户直接能感觉到

### [x] 1. 一次切换把 UI 线程堵住接近一秒

`WallpaperTransition.cs:408`（读文件）、`410`（解码）、`416`（重采样）、
`WallpaperService.cs:56`（`SystemParametersInfoW`）
调用点：`UI/TrayContext.cs:293`、`UI/TrayContext.cs:859`

一次切换，UI 线程从头堵到尾，四段串行：

1. `File.ReadAllBytes` 读一张 3~8 MB 的 UHD JPEG；
2. `Image.FromStream` **全尺寸解码**（3840×2160，中间位图约 33 MB）；
3. `DrawImage` + `HighQualityBicubic` 把它重采样到全屏；
4. `SystemParametersInfoW` —— 这一步 Windows 自己**再完整解码一遍并重新编码**，
   写出 `%APPDATA%\Microsoft\Windows\Themes\TranscodedWallpaper`。

满血 CPU 上 1~3 合计一百多毫秒、4 大概两三百毫秒，感觉不出来；
拔电降频到 800 MHz 时每段乘 3~5 倍，合计轻松过 1 秒。

**要说清楚的是第 4 段是引入淡入淡出之前就有的**：`ApplyIndexAsync` 一直在 UI 线程上直接调 `Apply`。
1~3 把这段既有卡顿差不多翻了一倍，但不是它凭空造出来的，修的时候两边一起收。

改法见「方案 A」和「方案 B」。

### [x] 2. 降频时偶尔整个渐变都不出现

`WallpaperTransition.cs:481`（`_timer.Start()`）→ `WallpaperTransition.cs:562` `OnTick`

**已实测确认**：拔电复现「没有渐变」的那几次，日志里 `fade: covered ...` 一条不缺——
盖子建成了、挂上去了、`Start()` 也调了，丢的只能是它后面的帧。以下不再是推测。

`Cover.Start()` 之后 `Run` 就返回了，`ApplyIndexAsync` 继续跑 `UpdateMenuState()`
（`UI/TrayContext.cs:304`），选择窗口再刷一次状态栏和格子，之后才轮到消息循环。

如果这一段、加上排在 `WM_TIMER` 前面的绘制消息，合计超过 500 ms，
那么**第一次 `WM_TIMER` 到达时 `elapsed` 已经 ≥ 500**，
`OnTick` 里 `progress >= 1.0` 直接 `Dispose()`——盖子瞬间消失，一帧中间态都没画过。
看起来就是「这次没有渐变」。

按时间算 alpha 的设计能扛住**抖动**，扛不住**整个时长内一次都不调度**。
降频时 UI 线程干同样的活要久好几倍，命中这个窗口的概率自然大得多，这正是「插电不复现」的原因。

这条路径是正常完成路径，一行日志都不写——所以「日志里没有 WARN」不是日志漏了，是符合预期的。

次要成因：`HoldMilliseconds = 120` 是拍脑袋定的。降频时资源管理器画新壁纸可能要 300 ms，
那就会先透出**旧**壁纸再啪地跳到新的，观感上同样是「没渐变」。见第 4 条。

改法见「方案 C」，配合第 4 条。

> **已修复。** 分两步：A + B 先把 UI 线程上的重活挪走，随后方案 C 把盖子整个搬到自己的一条
> UI 线程上，那条线程的消息队列里没有别的东西，`WM_TIMER` 不可能再被饿死——这才是治本的一步。
> 自适应 hold 顺带收掉了「先透出旧壁纸」那半个成因。
> `fade: done ticks=N` 现在能直接读出实际画了多少帧。

### [x] 3. 连续点击时渐变全部丢失

`WallpaperTransition.cs:85`（`Run` 开头的 `Cancel()`）

`Run` 每次都先把上一个盖子销毁。点得比 500 ms 快，就没有任何一次淡出能活到画出第一帧；
再叠上排队的鼠标消息把 `WM_TIMER` 彻底饿死（输入消息优先级高于定时器），结果是一次渐变都看不到。

两个页签的节流情况不同，值得分开记：

- **收藏页**：`UI/PickerForm.cs:607` `ApplyFavorite` 的 `_busy` 是纯同步的，
  置位和复位在同一个消息处理里，实际上拦不住任何东西；`TrayContext.ApplyFavorite`
  （`UI/TrayContext.cs:850`）也没有 `_busy` 检查。于是每一次点击都会完整应用一遍，
  背靠背地跑，`Cancel()` 一次次把前一个盖子杀掉。**这就是完全看不到渐变的那个场景。**
- **最近页**：`UI/PickerForm.cs:582` 的 `_busy` 会真的丢掉在途中的点击（`await` 期间）；
  但 `finally` 里复位之后，队列里下一条点击消息立刻被处理，同样赶在 `WM_TIMER` 前面。

**关于「进队列逐个渐变」这个期望，建议改成另一种做法**——理由写在「方案 D」里，
结论是：连点十下应该看到**一次**从第一张平滑淡到最后一张的过渡，而不是排队播十次。
「最后停留在我最后选的那张」这个诉求是满足的，而且更快。

---

## P1 · 相关的既有缺陷

### [x] 4. `HoldMilliseconds` 是一个固定的猜测值

`WallpaperTransition.cs:62`

120 ms 既没有依据也不自适应。`SystemParametersInfoW` 返回只代表转码完成，
资源管理器是在自己的线程上把新图画上去的，画完不通知任何人，也没有可靠信号可等。

改法：第 1 条把 SPI 挪到后台线程之后，可以顺手量它的耗时。
转码慢就说明这台机器此刻慢、资源管理器画得也会慢，于是
`hold = clamp(SPI 实测耗时, 150, 500)`。降频时自动放宽，满血时不多等——
比现在的固定 120 好，也比一刀切调大到 400 好。

### [x] 5. 收藏页的点击完全没有节流

`UI/PickerForm.cs:607`、`UI/TrayContext.cs:850`

见第 3 条的分析。这条独立于淡入淡出：即使没有渐变，连点收藏页也会让 UI 线程
背靠背地跑 N 次「解码 + 转码 + 写注册表」，中间一次都不喘气。

改法：和最近页对齐，或者按「方案 D」统一成「记住用户最后要的那张，当前这轮结束后补跑」——
和 `_rerunRequested` 是同一个套路。

> 注意这条与 TODO2 第 4 条（`_busy` 时点磁贴会假报成功）是**不同**的两件事：
> 那一条说的是定时刷新抬起 `TrayContext._busy` 时，`MoveToAsync` 开头就 `return` 却报成功；
> 这一条说的是收藏页压根没有节流。两条最好一起修，落点会有重叠。

---

## P2 · 可调性与诊断

### [~] 6. 淡出时长应该可调，并且可以顺手合并掉 `FadeTransition`

`WallpaperTransition.cs:65`、`AppConfig.cs:64` / `109` / `129`

380 ms 偏快——系统自己给幻灯片壁纸做的淡入淡出大约 1 秒。建议默认值改成 **600 ms**
（配 150 ms 的 hold），想更慢就 800。

既然要调，不如直接做成 INI 项 `FadeMilliseconds=600`，**0 表示关闭**——
这样刚加的 `FadeTransition` 布尔开关就可以删掉了，一个键顶两个，
也不必为了调快慢重新编译。README 的配置块和特性说明要同步改。

> **只做了默认值这一半**：`FadeMilliseconds` 常量改成 600，INI 项按要求不加，
> `FadeTransition=true/false` 保留原样。

### [x] 7. 淡出结束时没有任何可诊断的信息

`WallpaperTransition.cs:562` `OnTick`

第 2 条那种「一次都没调度到」的情况现在完全不留痕迹，只能靠猜。

改法：`Dispose` 或最后一次 tick 时补一行 Debug：
`fade: done ticks=1 elapsed=612`。`ticks=1` 一眼就能确诊饿死，以后再出问题不用推理。

---

## 方案

### 方案 A · 不再从文件重建旧图，直接抓壁纸层的像素

盖子要的就是「壁纸层现在长什么样」。与其读文件、解码、重采样，不如 `GetDC(WorkerW)` + `BitBlt`
直接拷贝那一层——WorkerW 是顶层窗口，DWM 给它单独的重定向表面，抓到的应当是**纯壁纸，
不含图标、不含窗口**。

- 耗时从几百毫秒降到**几毫秒**（一次显存内拷贝），第 1 条的一大半没了；
- 干掉那 33 MB 的解码位图，内存峰值直接砍半；
- **像素级完全一致**，原先「我们的重采样和资源管理器的不完全一样、起始帧可能有极细微跳变」
  这个瑕疵彻底消失；
- **不再需要复刻排版规则，六种填充方式全都能淡入淡出**，
  `TrayContext.FadeFrom`（`UI/TrayContext.cs:249`）、`WallpaperService.CanFadeFrom`
  （`WallpaperService.cs:96`）、`WallpaperTransition.GetFillRectangle` 可以一起删掉，代码反而变少。

**风险，必须实测**：DWM 下从别的进程的窗口 DC 里 `BitBlt`，个别情况会拿回全黑
（走 DirectComposition／硬件叠加层的窗口会这样）。壁纸是资源管理器用 GDI 画的，理论上没问题。
兜底：抓完采样几个点，全同色就判定失败，退回现在的「从文件渲染」那条路——
所以现有的 `Render` 代码留着当 fallback，不要删。

### 方案 B · 把 `SystemParametersInfoW` 挪到后台线程

`WallpaperService.cs:56`。这是 UI 线程上最后一段重活，也是唯一一段引入淡入淡出之前就存在的。
它不碰任何窗口，只写注册表 + 广播 `WM_SETTINGCHANGE`，放进 `Task.Run` 是安全的。

顺序变成：抓图（UI，几毫秒）→ 挂盖子 → `await Task.Run(SPI)`
← **这段时间 UI 完全空闲，而盖子正好把桌面挡得严严实实** → 淡出。

外溢：`ApplyIndexAsync` 本来就是 `async`，改动不大；
`TrayContext.ApplyFavorite`（`UI/TrayContext.cs:850`）是同步返回 `bool` 的，
要跟着改成 `async Task<bool>`，会往上传一层到 `UI/PickerForm.cs:607`。

### 方案 C · 让淡出不再受 UI 线程摆布

A + B 之后 UI 线程在淡出期间几乎没事干，但连点时排队的鼠标消息仍然会饿死 `WM_TIMER`。两个层次：

- **轻量版**：不用 `WM_TIMER`，改用线程池定时器 + `BeginInvoke` 投递。
  投递消息的取出顺序**排在输入和绘制之前**，抗饿死能力强得多。改动几行。
- **彻底版**：给盖子**一条自己的 UI 线程**——建窗口、跑消息循环、跑淡出全在那条线程上，
  淡完就退出。主线程再怎么忙都影响不到它。附带好处：跨进程挂子窗口带来的输入队列绑定
  也被隔离到这条短命线程上，不再挂在主 UI 线程上（这一点写在
  `WallpaperTransition` 的类注释里，可以一并更新）。

倾向**彻底版**：淡入淡出本来就是一段动画，它和主线程在忙什么没有任何关系，绑在一起只是省事。
`TrayContext.Dispose`（`UI/TrayContext.cs:316` 的 `WallpaperTransition.Cancel()`）
要改成向那条线程发信号，并带超时。

### 方案 D · 连点行为：让正在跑的淡出「接力」，而不是重来

把 `WallpaperTransition.cs:85` 的 `Cancel()` 去掉，改成：
**已经有盖子在淡出时，不建新的，直接换壁纸就行。**

这条特别划算，因为盖子上画的是「淡出开始那一刻屏幕的样子」，
而它淡掉之后露出来的是「此刻资源管理器正在画的东西」——中途换了几次壁纸，
最后露出来的自然就是最后那张。所以连点十下的结果是
**一次从第一张到最后一张的、完整平滑的淡入淡出**，中间那几张的渲染成本全省了。

这和「进队列逐个渐变」不一样，建议按这个来：连点十下要坐等 5 秒动画才能停在最后一张，
那不叫流畅叫迟钝；而且中途每一张都只露一瞬间，看到的也只是一串闪烁。

**但**第 5 条（收藏页没有节流）不解决的话，连点时 UI 线程照样要背靠背跑 N 次转码，
改多少渲染都白搭。两条一起修。

---

## 建议的施工顺序

分两次做，每次都是能独立验证的完整状态：

1. ~~**A + B + 第 4 条（自适应 hold）+ 第 7 条（收尾日志）**~~ — 已完成
   —— 直接消掉卡顿和「降频时偶尔没渐变」。改动集中在 `WallpaperTransition` 内部，
   加上 `ApplyFavorite` 变异步这一处外溢。做完 A 之后记得回头改 README：
   「仅填充模式」那几句不再成立。
2. ~~**C（彻底版）+ D + 第 5 条 + 第 6 条**~~ — 已完成（第 6 条只改了默认值，INI 项按要求不做）
   —— 解决连点，并把时长交给 INI。第 5 条动的是 `TrayContext` / `PickerForm`，
   和 TODO2 第 4 条落点重叠，可以一并收掉。

---

## 前置确认：已完成（2026-09-06）

把 INI 改成 `LogLevel=Debug`、拔掉电源、切了几次壁纸，**包括肉眼完全没看到渐变的那几次，
日志里 `fade: covered ...` 始终都在**，没有出现过 `fade: skipped, ...`、`fade: no workerw ...`，
也没有任何一条 `... cutting instead`。

这落在原先列的两种结果里的第一种：**盖子确实挂上去了，丢的是定时器。**
第 2 条的判断成立——`Cover.Start()` 之后 `WM_TIMER` 在整整 500 ms 里一次都没被取出来，
第一次 `OnTick` 进来时 `progress` 已经 ≥ 1.0，直接 `Dispose()`，一帧中间态都没画过。

由此定下来的几件事：

- **不必再去查 WorkerW、填充模式或 `CanFadeFrom` 那条判定**，建盖子这条路本身是好的；
  方案 A / B / C 都对症，按「建议的施工顺序」直接开工。
- **第 7 条（收尾日志）的优先级应当提到和第 2 条一样高。** 现在只知道「进了」，
  不知道「跑了几帧」；补上 `fade: done ticks=1 elapsed=612` 之后，
  同类问题看一眼 `ticks` 就能确诊，不用再拔一次电复现。
- 分清主次：方案 A（抓屏代替解码）在这里是**减压**，真正治本的是方案 C——
  UI 线程再空，只要淡出还挂在 `WM_TIMER` 上，连点时的输入消息照样能把它饿死。
  A + B 先做是因为它们顺带把第 1 条的卡顿也收掉了，而且改动都关在 `WallpaperTransition` 内部。
